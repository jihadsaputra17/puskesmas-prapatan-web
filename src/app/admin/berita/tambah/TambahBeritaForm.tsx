"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import AdminFeedback from "@/components/admin/AdminFeedback";
import { formatFieldErrors, newsSchema } from "@/lib/admin-schemas";

export default function TambahBeritaForm() {
 const router=useRouter(); const [pending,setPending]=useState(false); const [feedback,setFeedback]=useState<{type:"success"|"error";message:string}|null>(null); const [fields,setFields]=useState<Record<string,string>>({});
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); setFeedback(null); const form=new FormData(e.currentTarget); const parsed=newsSchema.safeParse(Object.fromEntries(form)); if(!parsed.success){setFields(formatFieldErrors(parsed.error)); setFeedback({type:"error",message:"Periksa isian formulir."});return;} setFields({});setPending(true);try{const r=await fetch("/api/berita",{method:"POST",body:form});const result=await r.json().catch(()=>({}));if(!r.ok||result.error){setFields(result.fields||{});setFeedback({type:"error",message:result.error||"Gagal mempublikasikan berita."});}else{router.push("/admin/berita");router.refresh();}}catch{setFeedback({type:"error",message:"Terjadi kesalahan jaringan saat menyimpan berita."});}finally{setPending(false)}}
 const err=(id:string)=>fields[id]?<p id={`${id}-error`} className="mt-1 text-sm text-red-700">{fields[id]}</p>:null; const described=(id:string)=>fields[id]?`${id}-error`:undefined;
 return <div className="max-w-3xl rounded-xl border border-slate-200 bg-white p-8 shadow-sm"><div className="mb-8 flex justify-between border-b pb-4"><h1 className="text-2xl font-bold">Tambah Berita Baru</h1><Link href="/admin/berita">← Batal</Link></div><AdminFeedback result={feedback}/><form onSubmit={submit} className="space-y-6">{[["title","Judul Berita"],["slug","Tautan / Slug"],["excerpt","Ringkasan Singkat"],["content","Isi Berita Lengkap"]].map(([id,label])=><div key={id}><label htmlFor={id} className="block font-medium">{label}</label>{id==="excerpt"||id==="content"?<textarea id={id} name={id} rows={id==="content"?8:3} aria-describedby={described(id)} className="w-full rounded border p-2"/>:<input id={id} name={id} aria-describedby={described(id)} className="w-full rounded border p-2"/>}{err(id)}</div>)}<div><label htmlFor="template">Template</label><select id="template" name="template" className="w-full rounded border p-2"><option value="standard">Standar</option><option value="hero-overlay">Hero Banner</option><option value="minimalist">Minimalis</option></select></div><div><label htmlFor="image_url">URL gambar sampul (opsional)</label><input id="image_url" name="image_url" type="url" inputMode="url" placeholder="https://..." aria-describedby={described("image_url")} className="w-full rounded border p-2"/>{err("image_url")}</div><button disabled={pending} className="rounded bg-teal-600 px-5 py-3 font-semibold text-white disabled:bg-slate-400">{pending?"Memproses...":"Publikasikan Berita"}</button></form></div>;
}
