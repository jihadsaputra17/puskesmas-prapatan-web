"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import AdminFeedback from "@/components/admin/AdminFeedback";
import { formatFieldErrors, newsSchema } from "@/lib/admin-schemas";
type Berita = Record<string, string | undefined> & { id: string };
export default function EditBeritaForm({berita}:{berita:Berita}){const router=useRouter();const[pending,setPending]=useState(false);const[feedback,setFeedback]=useState<{type:"success"|"error";message:string}|null>(null);const[fields,setFields]=useState<Record<string,string>>({});async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setFeedback(null);const form=new FormData(e.currentTarget);const parsed=newsSchema.safeParse(Object.fromEntries(form));if(!parsed.success){setFields(formatFieldErrors(parsed.error));setFeedback({type:"error",message:"Periksa isian formulir."});return;}setFields({});setPending(true);try{const r=await fetch(`/api/berita/${berita.id}`,{method:"PUT",body:form});const result=await r.json().catch(()=>({}));if(!r.ok||result.error){setFields(result.fields||{});setFeedback({type:"error",message:result.error||"Gagal memperbarui berita."});}else{router.push("/admin/berita");router.refresh();}}catch{setFeedback({type:"error",message:"Terjadi kesalahan jaringan saat menyimpan berita."});}finally{setPending(false)}}const error=(id:string)=>fields[id]&&<p id={`${id}-error`} className="mt-1 text-sm text-red-700">{fields[id]}</p>;const aria=(id:string)=>fields[id]?`${id}-error`:undefined;return <div className="max-w-3xl rounded-xl border bg-white p-8"><div className="mb-8 flex justify-between border-b pb-4"><h1 className="text-2xl font-bold">Edit Berita</h1><Link href="/admin/berita">← Batal</Link></div><AdminFeedback result={feedback}/><form onSubmit={submit} className="space-y-6">{[["title","Judul Berita"],["slug","Tautan / Slug"],["excerpt","Ringkasan Singkat"],["content","Isi Berita Lengkap"]].map(([id,label])=><div key={id}><label htmlFor={id}>{label}</label>{id==="excerpt"||id==="content"?<textarea id={id} name={id} defaultValue={berita[id]} rows={id==="content"?8:3} aria-describedby={aria(id)} className="w-full rounded border p-2"/>:<input id={id} name={id} defaultValue={berita[id]} aria-describedby={aria(id)} className="w-full rounded border p-2"/>}{error(id)}</div>)}<div><label htmlFor="template">Template</label><select id="template" name="template" defaultValue={berita.template||"standard"} className="w-full rounded border p-2"><option value="standard">Standar</option><option value="hero-overlay">Hero Banner</option><option value="minimalist">Minimalis</option></select></div><div><label htmlFor="image_url">URL gambar sampul (opsional)</label><input id="image_url" name="image_url" type="url" defaultValue={berita.image_url} aria-describedby={aria("image_url")} className="w-full rounded border p-2"/>{error("image_url")}</div><button disabled={pending} className="rounded bg-teal-600 px-5 py-3 font-semibold text-white disabled:bg-slate-400">{pending?"Menyimpan...":"Simpan Perubahan Berita"}</button></form></div>}
